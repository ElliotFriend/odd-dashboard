# Clean MAD (winget removed) — horizon 2026-07-07

Extract rebuilt 2026-07-15; latest `eco_mads.day` = **2026-07-07**. 28-day window =
2026-06-10 → 2026-07-07 inclusive.

Sanity check: distinct devs over the window across all repos = **3,782**, matches
`eco_mads.all_devs` exactly. "Clean" = drop devs whose ONLY window activity was
`stellar/winget-pkgs` (repo id `1770468`, fork-attribution bug — see
`winget-pkgs-phantom-devs` memory); anyone who also touched a real repo stays.

## Cleanly recomputed from the extract (`repo_day`, 28d window)

| metric | official | winget-only | **clean** |
|---|---:|---:|---:|
| MAD (`all_devs`, 28d) | 3,782 | 824 | **2,958** |
| num_commits (28d) | 91,584 | 10,826 | **80,758** |

- winget touchers in window: **825** — 824 winget-only (drop), **1** also touched a real repo (keep).
- winget = **22%** of MAD, **12%** of windowed commits.
- Bug still accruing: 486 (Jun 29) → 527 (Jul 1) → 793 (Jul 3) → 795 (Jul 5) → 805 (Jul 6) → **824 now**.
- Recent daily activity on repo 1770468 (devs / commits):
  - Jul 7: 129 / 503
  - Jul 6: 81 / 255
  - Jul 5: 52 / 168
  - Jul 4: 61 / 264
  - Jul 3: 98 / 381
  - Jul 2: 134 / 477
  - Jul 1: 100 / 782
  - Jun 30: 74 / 308

## Contribution ranks — VERIFIED against canonical (snapshot `20260715T125411`)

Recomputed from canonical `eco_developer_contribution_ranks` (ecosystem_id=7,
horizon day 2026-07-07). Official totals match `eco_mads` exactly (512 / 2,742 / 528).
"winget-only" = the 824 devs whose only 28d-window activity was repo 1770468;
intersected with each canonical rank's dev set on the horizon day. All 824 carry a
rank (68 + 531 + 225).

| rank | official | winget-only | **clean** |
|---|---:|---:|---:|
| full_time | 512 | **68 (13%)** | **444** |
| part_time | 2,742 | 531 (19%) | **2,211** |
| one_time | 528 | 225 (43%) | **303** |

**full_time is NO LONGER insulated.** On Jul 1 the canonical check found 0 winget
full-time; ~2 weeks of continuous daily winget PR-farm activity has now pushed **68**
of them over the 84d full-time threshold (most show points=10 across 28d & 56d).
one_time is still the most polluted proportionally (43%).

## Quote with caveats — NOT recomputable

The exclusive/multichain split needs cross-ecosystem data (not in the Stellar-only
extract; winget devs propagate into other-eco rollups). Tenure is by first-ever commit.

| metric | official 07-07 | note |
|---|---:|---|
| exclusive_devs | 2,562 | can't split winget out — needs cross-eco data |
| multichain_devs | 1,220 | winget-only devs propagate into other-eco rollups → likely inflated |
| devs_0_1y / 1_2y / 2y+ | 2,776 / 479 / 527 | winget upstream devs are new-to-Stellar → concentrated in 0_1y |

**Headline: clean MAD ≈ 2,958 vs official 3,782. Clean full_time ≈ 444 vs official 512.**
