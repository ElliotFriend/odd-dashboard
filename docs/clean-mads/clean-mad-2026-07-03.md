# Clean MAD — winget-pkgs removed

**Extract rebuilt:** 2026-07-11 · **Data horizon:** 2026-07-03 · **28d window:** 2026-06-06 → 2026-07-03

Sanity check: distinct devs over the 28d window in `repo_day` = 3,742 = `eco_mads.all_devs`. ✓

## Clean MAD

| metric | official | clean (excl `stellar/winget-pkgs`, repo 1770468) |
|---|---:|---:|
| **MAD (all_devs)** | 3,742 | **2,949** |
| exclusive_devs | 2,521 | — |
| multichain_devs | 1,221 | — |
| num_commits (28d) | 89,153 | — |

- winget touchers in window: **794** → **793 winget-only dropped**, **1 kept** (touched winget + a real repo).
- winget-only devs = **21.2%** of MAD.
- Trend worsening: 486 (14.2%) at Jun 29 horizon → 527 at Jul 1 → **793 at Jul 3**.
- Still accruing, no taper. Last winget-active day = horizon (Jul 3). Recent daily activity on repo 1770468:
  - Jul 3: 98 devs / 381 commits
  - Jul 2: 134 devs / 477 commits
  - Jul 1: 100 devs / 782 commits
  - Jun 30: 74 devs / 308 commits
  - Jun 29: 108 devs / 350 commits

## Tenure / contribution ranks (official — clean split not recomputable from this extract)

| metric | value | note |
|---|---:|---|
| devs_0_1y | 2,743 | |
| devs_1_2y | 478 | |
| devs_2y_plus | 521 | |
| full_time_devs | 491 | **clean** — winget devs never classified full-time |
| part_time_devs | 2,725 | ~10% winget noise |
| one_time_devs | 526 | **badly polluted** (~half is winget noise) |

`exclusive_devs`/`multichain_devs` and the 84d contribution ranks can't be split by winget from `repo_day` alone (needs canonical `eco_developer_contribution_ranks`, not in the extract). `full_time_devs` safe to quote as-is; `one_time_devs` is the compromised metric.

## Method

Clean recount = distinct `dev` over the 28d window from `repo_day` excluding `repo_id = 1770468`. Devs whose only window activity was winget drop; anyone who also touched a real repo stays. Full recount matches `eco_mads.all_devs` (3,742) as sanity check.

**Bottom line: clean MAD ≈ 2,949 vs official 3,742.**

---

Background: `stellar/winget-pkgs` is a fork of Microsoft's winget-pkgs sitting under the `stellar/` GitHub org; crypto-ecosystems maps it to Stellar, so its upstream Microsoft/winget contributors get counted as Stellar devs (fork-attribution bug). EC began counting it ~2026-06-17. Package-manifest PR-farm signature: hundreds of one-off devs, single repo, sharp on/off. Clears ~28d after activity stops or when EC fixes the mapping.
