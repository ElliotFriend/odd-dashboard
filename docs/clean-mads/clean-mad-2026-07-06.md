# Clean MAD (winget removed) — horizon 2026-07-06

Extract rebuilt 2026-07-14; latest `eco_mads.day` = **2026-07-06**. 28-day window =
2026-06-09 → 2026-07-06 inclusive.

Sanity check: distinct devs over the window across all repos = **3,753**, matches
`eco_mads.all_devs` exactly. "Clean" = drop devs whose ONLY window activity was
`stellar/winget-pkgs` (repo id `1770468`, fork-attribution bug — see
`winget-pkgs-phantom-devs` memory); anyone who also touched a real repo stays.

## Cleanly recomputed from the extract (`repo_day`, 28d window)

| metric | official | winget-only | **clean** |
|---|---:|---:|---:|
| MAD (`all_devs`, 28d) | 3,753 | 805 | **2,948** |
| num_commits (28d) | 90,671 | 10,703 | **79,968** |

- winget touchers in window: **806** — 805 winget-only (drop), **1** also touched a real repo (keep).
- winget = **21%** of MAD, **12%** of windowed commits.
- Bug still accruing: 486 (Jun 29) → 527 (Jul 1) → 793 (Jul 3) → 795 (Jul 5) → **805 now**.
- Recent daily activity on repo 1770468 (devs / commits):
  - Jul 6: 81 / 255
  - Jul 5: 52 / 168
  - Jul 4: 61 / 264
  - Jul 3: 98 / 381
  - Jul 2: 134 / 477
  - Jul 1: 100 / 782
  - Jun 30: 74 / 308

## Quote with caveats — NOT recomputable from this extract

Extract holds only `eco_mads` presentation values for these. Contribution ranks use
an 84-day window + contribution intensity; tenure is by first-ever commit; the
exclusive/multichain split needs cross-ecosystem data. None of that is in the
Stellar-only extract, so winget can't be subtracted here directly.

| metric | official 07-06 | note |
|---|---:|---|
| exclusive_devs | 2,537 | can't split winget out — needs cross-eco data |
| multichain_devs | 1,216 | winget-only devs propagate into other-eco rollups → likely inflated |
| full_time_devs | 507 | **safe as-is** — winget devs classified 0 full-time (prior canonical check) |
| part_time_devs | 2,729 | ~10% winget noise (last canonical check) |
| one_time_devs | 517 | **badly polluted** — ~half was winget noise on Jul 1; real ≈ half |
| devs_0_1y / 1_2y / 2y+ | 2,752 / 476 / 525 | winget upstream devs are new-to-Stellar → concentrated in 0_1y |

**Headline: clean MAD ≈ 2,948 vs official 3,753.**

Full clean exclusive/multichain and rank splits need the canonical ODD tables
(`eco_developer_contribution_ranks`, cross-ecosystem), not this extract.
