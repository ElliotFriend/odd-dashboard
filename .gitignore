# secrets — never commit
.env
.env.*

# macOS
.DS_Store

# python (root)
.venv
__pycache__/

# data extract (built artifact, lives at repo root)
*.duckdb
*.duckdb.wal

# scratch — large downloaded data (e.g. GH Archive hourly dumps), never commit
scratch/

# js — node_modules anywhere; SvelteKit build artifacts are in web/.gitignore
node_modules
