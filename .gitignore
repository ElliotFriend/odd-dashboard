# macOS
.DS_Store

# python (root)
.venv
__pycache__/

# data extract (built artifact, lives at repo root)
*.duckdb
*.duckdb.wal

# scratch — large downloaded data (e.g. GH Archive hourly dumps), never commit
scratch/

# js (SvelteKit app in /web)
node_modules
web/.svelte-kit
web/build
