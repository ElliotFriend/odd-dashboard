[project]
name = "stellar-odd-dashboard"
version = "0.1.0"
description = "Analyze and see into Stellar developer activity"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "duckdb>=1.5.3",
]

[dependency-groups]
dev = [
    "pandas>=3.0.3",
    "pytz>=2026.2",
]
